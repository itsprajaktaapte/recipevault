apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: recipevault
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 
  type: ClusterIP                # internal only — ingress handles public access